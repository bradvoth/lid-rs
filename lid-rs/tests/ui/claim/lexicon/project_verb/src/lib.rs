//! A verb the project's file defines and the base does not is admitted, with
//! the project's templates.
//!
//! The claim below takes that verb and names no response object. Its file gives
//! the verb a template rather than `*`, so the failure is the missing object
//! link and not an undefined verb: the message is the evidence that the verb was
//! admitted, and admitted as its own file writes it.

/// When the [`Index`] is written, the run shall stage.
#[derive(lid_rs::Spec)]
pub struct AClaimTakingTheProjectsOwnVerb;
