//! A term the project's `[prohibited] extra` supplies is prohibited as the
//! built-in list's terms are, and the message names the term and the file that
//! supplied it — the file being where the prohibition can be argued with.

/// When the [`Turn`] ends, the run shall stop promptly.
#[derive(lid_rs::Spec)]
pub struct AClaimUsingATermTheProjectForbids;
