[package]
name = "claim-fixture-outer-pkg"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
lid-rs = { path = "../../../../.." }

# `inner/` is not a dependency, and cannot be one: it opens a workspace of its
# own, and cargo loads a member's path dependencies whatever the root excludes.
# The harness checks it in a run of its own.
